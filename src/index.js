// Render may default to running `src/index.js` — forward to the existing server entrypoint.
// This file simply requires the top-level `server.js` so the app starts normally.

require('../server');
